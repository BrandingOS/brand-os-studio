# 08 — Legacy & Dead Code Classification

> Agent: B3-dead · Date: 2026-08-08 · Branch: `new-ui` @ `46ffb41`
> Method: multi-signal verification. **No candidate is classified dead from fewer than 3
> independent signals.** Signal key:
> **S1** static imports (grep incl. barrels) · **S2** route reachability (02-ROUTES) ·
> **S3** dynamic/lazy imports · **S4** registries/config-driven loading (dev-features
> registry, core/modules registry) · **S5** tests referencing it · **S6** runtime DI/service
> consumers · **S7** database dependencies · **S8** replacement feature exists ·
> **S9** product-surface relevance (01) · **S10** git last touch.
> Note: `features/dev-features/features-registry.ts` holds only route *strings* + entry-file
> *paths* (zero `import(` calls — VERIFIED) — a registry hit is documentation, not execution.
> READ-ONLY audit: nothing was deleted.

---

## A. CONFIRMED DEAD (delete-safe pending owner sign-off)

| Candidate | Layer | Signals checked | Classification | Conf. | Replacement | Notes |
|---|---|---|---|---|---|---|
| `src/domains/dashboard/` | UI | S1 (0 importers), fs (contains **only `.DS_Store`**), S10 (2025-08-14) | CONFIRMED DEAD | 100% | whole app | Empty husk folder. |
| `src/core/modules/` (`registry.ts` + `types.ts`) | config/registry | S1 (0 importers outside itself — VERIFIED), S4 (registry consumed by nothing), S2 (advertises unmounted `/tools/colors` + pre-redirect `/dashboard/brand/*` prefixes), S10 (2026-04-04) | CONFIRMED DEAD | 100% | route table in `App.tsx` | **Answers 02 open-Q7: nothing consumes the routePrefixes.** Safe to delete wholesale. |
| `src/shared/routing/FirstBrandRedirect.tsx` | routing | S1 (self-only), S2 (mounted nowhere), S8 (flat cosmos tabs still render placeholders instead), S10 (2026-04-23) | CONFIRMED DEAD | 100% | none (intended UX never wired) | Encodes the *intended* fix for the mock-data `/setup` tabs — preserve the idea, not the file. |
| `src/shared/hooks/useRealtimeComments.ts` | hook/service | S1 (0 importers), S6 (SupabaseCommentsService also unconsumed), S7 (comments feature uses localStorage `commentsStore`), S10 (2026-04-12) | CONFIRMED DEAD | 100% | `features/comments` local store | Part of the abandoned realtime-comments backend push. |
| `src/shared/hooks/useDataSync.ts` | hook | S1 (**0 callers** — VERIFIED grep), S6 (its `syncToSupabase` path superseded), S8 (`useAuth` now calls `loadAll()` + `migrateLocalStorageToSupabase` directly), S10 (2026-04-02, oldest orphan found) | CONFIRMED DEAD | 100% | inline logic in `useAuth.ts` | **Corrects 03 §1 gen-C:** onboardingStore has only 2 live importers (`useBrandPreview`, `useAuth`); the third, useDataSync, is itself dead. |
| App.tsx dead lazy import: `DashboardRoute` (:70) → `src/pages/dashboard/index.tsx` **+ its exclusive chain**: `features/dashboard/components/DashboardMain.tsx`, `ContinueSurface.tsx`, `DashboardSidebar.tsx`, **entire `features/dashboard/v5/`** (`HomeV5`, sections `WorkspaceHero`, `ActivityFeed`, `BrandsGridV5`, `QuickActionsRail`, `RecentAssetsRow`) | UI | S1 (each file's only importers are inside the chain — VERIFIED per file), S2 (never mounted; App.tsx:358 comment "Phase 6 removes it"), S3 (lazy-declared, unused), S8 (`pages/workspace/Home.tsx` is the live `/dashboard`), S5 (0 tests), S10 (2026-04-16) | CONFIRMED DEAD | 98% | `pages/workspace/Home` + `WorkspaceShellAlt` | **Do NOT delete `DashboardLayout.tsx`/`DashboardNavbar.tsx` with it** — those are live (see §D). v5 sections dying also removes one of the two `AssistantTrigger` call sites (03 §10). |
| App.tsx dead lazy import: `LearnPage` (:120) → `src/pages/learn/index.tsx` | UI | S1 (only App.tsx + dev-registry string), S2 (unmounted; `/learn` mounts `pages/workspace/Learn`), S3, S8, S10 (2026-04-10) | CONFIRMED DEAD | 98% | `pages/workspace/Learn.tsx` | |
| App.tsx dead lazy import: `StandaloneEditorPage` (:140) → `src/pages/editor/index.tsx` | UI | S1 (only App.tsx), S2 (unmounted since Phase 5 swap), S3, S8 (`/editor` launcher → unified editor), S10 (2026-04-13) | CONFIRMED DEAD | 98% | `pages/editor-launcher.tsx` | Renders `OptimizedDesignEditor` with a synthetic "standalone" brand. Deleting it removes 1 of the 2 importers of `features/editor/components`. |
| App.tsx dead lazy import: `TemplatesMarketplacePage` (:161) → `src/features/templates/v5/TemplatesMarketplacePage.tsx` (**sole file in `templates/v5/`**) | UI | S1 (only App.tsx + dev-registry string), S2 (unmounted), S3, S8 (`pages/workspace/Templates` at `/templates`), S4 (features inventory page itself labels it `status: 'orphan'`, `pages/dashboard/features/index.tsx:128`), S10 (2026-04-18) | CONFIRMED DEAD | 98% | `pages/workspace/Templates` | Folder `features/templates/v5/` can go entirely. |
| `features/brand/components/AppShellCanvaLayout.tsx` | UI | S1 (importers: barrel `features/brand/index.ts` + self only; no named use of the export anywhere), S2 (no route renders it), S8 (`BrandRouteLayout`/`WorkspaceShell` shells), S10 (folder-level) | CONFIRMED DEAD | 92% | scope shells | `CanvaSidebar` itself is **live** via `guidelines/canvas.tsx` — only the layout wrapper is dead. |
| `features/brandkit/components/BrandKitHub.tsx` + `pages/dashboard/brand/[slug]/brandkit/index.tsx` | UI | S1 (hub's only importers: the orphan page + barrel), S2 (`/a/:slug/brandkit` without moduleId is a `<Navigate>` — `BrandKitRedirect`, App.tsx:696; the index page is mounted nowhere), S8 (Classic `brand-kit-alt/BrandKitPage` + Studio `BrandKitCosmosPage`), S10 (2026-05-05 folder) | CONFIRMED DEAD | 95% | both kit hubs | The *module canvas* sibling (`[moduleId].tsx` → `BrandKitModuleView`) stays live (§C). |
| Edge fn `supabase/functions/generate-description` | backend | S1 (0 callers in `src/` + `landingpage/src/` — VERIFIED), S7 (rate-limit rows only), S8 (onboarding-v4 `parseDescription.ts` calls Anthropic directly), S10 (2026-04-27) | CONFIRMED DEAD (code-side) | 95% | `onboarding-v4/services/parseDescription.ts` | Deployed-in-prod status UNKNOWN — undeploy check needed. |
| Edge fn `supabase/functions/fetch-url-preview` | backend | S1 (0 callers), S7 (only migration 008 rate limits mention it), S10 (2026-04-27) | CONFIRMED DEAD (code-side) | 95% | none | Same undeploy caveat. |
| Edge fns `finalize-onboarding-assets` + `cleanup-onboarding-scratch` + migration `007_onboarding_v3.sql` (bucket `onboarding-scratch`, table `onboarding_rate_limits`) | backend + DB | S1 (0 SPA callers), S7 (bucket/table referenced only by these two fns), S8 (onboarding-v4 embeds assets as data URLs in brand JSON), S2 (v3 URLs are `<Navigate>` shims), S10 (2026-04-20) | CONFIRMED DEAD (code-side) | 95% | onboarding-v4 localStorage posture | The whole v3 upload pipeline: 2 functions + 1 bucket + 1 table + RLS. Prod-deployed status UNKNOWN. |
| `src/pages/onboarding-v3/{index,create,preview}.tsx` (redirect shims) | routing | S1/S2 (mounted, but **zero non-dev inbound links** — 02 §1.2), S8 (`/onboard-brand`), S4 (only dev-registry references), S10 | CONFIRMED DEAD (link-dead shims) | 90% | `/onboard-brand` | Only external bookmarks argue for keeping; 02 open-Q2. The other onboarding shims (`/onboarding`, `/onboarding-brand`, `/onboarding-v4/*`) are still *linked from live code* — see §B. |
| ~~`features/brand/components/BrandSidebar.tsx`~~ | UI | S10: **already deleted** in `fa54b0d` (2026-04-23, "delete confirmed-dead BrandSidebar + OnboardingShell") | RESOLVED (gone) | 100% | `AppRail` | **CLAUDE.md is stale** — it still says the file exists as "dead code kept for reference". |
| ~~`src/features/landing-v2/`~~ | UI | fs: folder does not exist | RESOLVED (gone) | 100% | `landingpage/` | CLAUDE.md "legacy landing v2 kept for reference" is stale. |
| ~~`features/brandkit/components/editor/`~~, ~~`FabricRenderer.ts`~~ | UI | fs: neither exists (find returns nothing) | RESOLVED (gone) | 100% | unified editor | Step-9 deletions actually happened. |

## B. HIGH-CONFIDENCE DEAD (functionally dead; one weak thread keeps them warm)

| Candidate | Layer | Signals checked | Classification | Conf. | Replacement | Notes |
|---|---|---|---|---|---|---|
| `pages/dashboard/admin/brands` + `pages/dashboard/admin/analytics` | UI | S2 (mounted, ProtectedRoute only — **no role gate**), S1 (only inbound link is the dead `DashboardSidebar`), S8 (`/admin/*` moderator-gated subtree), S10 (**2025-08-27 — untouched for ~1 year**, oldest live-mounted code found) | HIGH-CONFIDENCE DEAD | 90% | `features/admin` `/admin/*` | URL-reachable by any authed user; delete is also a small security win (02 open-Q1). |
| 5 Supabase adapter registrations: `SupabaseAssetsService`, `SupabaseCommentsService`, `SupabaseApprovalsService`, `SupabaseNotificationsService`, `SupabaseActivityService` | service/DI | S6 (`SERVICE_KEYS.{ASSETS,COMMENTS,APPROVALS,NOTIFICATIONS,ACTIVITY}` have **zero consumers outside `src/core`** — VERIFIED), S1 (class names appear only in boot.ts + own file), S7 (tables exist, migration 002), S8 (features use local zustand stores; admin reads `activity_log` via its own `adminService`, not the adapter) | HIGH-CONFIDENCE DEAD (unconsumed registrations — they *are* instantiated at auth) | 95% | local stores / `adminService` / `activityService` | WORKSPACES is the only auth-registered adapter with a live consumer (`workspaceStore`). |
| `remotion/` (brandingos-reel) | tooling | S1 (0 refs in `src/`, `supabase/`, root `package.json`), S2 (no route), S9 (standalone marketing-asset generator per 01 §10), S10 (2026-04-15) | HIGH-CONFIDENCE DEAD *as product code* | 85% | n/a | Deliberately standalone; "dead" here means "not part of the app build" — keep or archive per owner intent. |
| `docs/` archive subfolders describing superseded systems: `versions/` (v3.0 PRD/QA, 2026-04-03), `ux-v2/` (2026-04-23), `onboard/` (April HTML mockups, 2026-04-22), `guidelines/` (2026-04-02), `services/` (2026-04-03), `figma-ref/` (2026-04-03), `screenshots/` (2026-04-06) | docs | S10 (all pre-May), S8 (systems they describe rewritten: onboarding→v4, guidelines→Chronicle), S9 (01 lists none as live surface) | HIGH-CONFIDENCE DEAD (stale docs) | 85% | `docs/codebase-intelligence/`, `docs/ux-redesign/` (still CLAUDE.md-canonical) | Archive candidates, not code risk. |
| Root clutter: **32 loose `*.png`** screenshots, `MOCKUP_STUDIO_ADAPTATION_PLAN.md`, `docs/BRANDOS-DASHBOARD.html`, `Brands/` binary folder (PDFs + per-brand assets, **not git-ignored** — 01 §10) | assets | S1 (no code references found), S9, S10 | HIGH-CONFIDENCE DEAD (housekeeping) | 85% | n/a | `Brands/` risks committing raw designer binaries. |

## C. PROBABLY LEGACY (live today, superseded or frozen — shrink, don't grow)

| Candidate | Layer | Signals checked | Classification | Conf. | Replacement | Notes |
|---|---|---|---|---|---|---|
| `features/guidelines` (plural, ~52 files/10k LOC) | UI **live**, superseded | S2 (3 mounts: `/a/:slug/guideline`, `/b/:slug/guidelines/canvas`, `/b/:slug/brand-guides`), S1 (6 external importers incl. **public showcase page** + `exportService` + `brand-to-slides` + `/dashboard/templates`), S7 (live `guideline_presentations` Supabase table — one of the few real Supabase feature paths), S8 (Chronicle at `/b/:slug/guideline`), S10 (2026-04-16) | PROBABLY LEGACY | 90% | `features/guideline` (Chronicle) | CLAUDE.md debt #10. Cannot delete: it feeds the public showcase and guideline exports. |
| `features/editor/components` (`OptimizedDesignEditor`) | UI | S2 (mounted `/editor/design/:slug`), S1 (2 importers: `pages/editor/design.tsx` live + `pages/editor/index.tsx` **dead**), S8 (unified editor), S9 (documented carve-out, coupled to `stable/editable-export-v1`), S10 (2026-04-16) | PROBABLY LEGACY (kept carve-out) | 90% | `features/editor` unified | Deleting §A's `StandaloneEditorPage` halves its import surface. |
| G1 workspace pages: `pages/dashboard/{brands,activity,templates,features,logo-maker}` | UI | S2 (all mounted), S1 (inbound: brands←Home cards?, activity←`NotificationBell:113`, templates←`searchIndex:40`+`CanvaSidebar:53`, features←`AppRail:94`, logo-maker←`DashboardShell` page) , S8 (v2 workspace pages), S10 (mixed) | PROBABLY LEGACY | 80% | `pages/workspace/*` | `/dashboard/templates` still imports `features/guidelines`. `pages/dashboard/features/index.tsx` is itself a curated feature inventory that self-labels other surfaces `'orphan'` — useful audit artifact. |
| Onboarding redirect shims still linked from live code: `/onboarding` (5 live linkers), `/onboarding-brand` (`BrandChooserDialog:225`), `/onboarding-v4/*` | routing | S2 (mounted `<Navigate>` one-liners, 6–9 LOC each), S1 (live inbound links), S8 | PROBABLY LEGACY (keep until links repointed) | 95% | `/onboard-brand` | Repoint the 6 call sites, then these join §A with `/onboarding-v3/*`. |
| `shared/store/onboardingStore.ts` | store | S1 (2 live importers after the useDataSync correction: `useBrandPreview`, `useAuth`), S8 (onboarding-v4 has its own store), S10 | PROBABLY LEGACY | 80% | `onboardingV4Store` | Pre-v4 store on life support inside auth glue. |
| `features/brandkit` module-canvas UI (`BrandKitModuleView`, module components, `CanvasEditor`) | UI legacy / domain live | S2 (`/a/:slug/brandkit/:moduleId` mounted Classic), S1 (45 external importer files for the domain layer), S8 (Studio brand-kit), S10 (2026-05-05) | PROBABLY LEGACY (UI) / CURRENTLY USED (domain) | 90% | `features/brand-kit` | The flagship "dead UI over live domain" case — see §E. |
| `features/marketplace` | UI | S2 (mounted `/marketplace`), S1 (inbound: `searchIndex:44` only), S4 (self-labeled `'orphan'` in features inventory :129), S10 (2026-04-08, tied-oldest feature) | PROBABLY LEGACY | 75% | unclear | Route + command palette keep it technically reachable. |
| `features/blocks` | UI | S2 (mounted `/b/:slug/guidelines/blocks`), S1 (**inbound links exist** — corrects 03: `brand-kit-alt/BrandBookSection:49`, Classic share page `:148`, `searchIndex:68`), S10 (2026-04-08) | PROBABLY LEGACY / frozen experiment | 70% | Chronicle? undecided | Not link-dead as previously suspected. |
| `features/collaboration` | UI | S1 (1 live importer: Classic `BrandHomePage`), S2 (via `/a/:slug/setup`), S10 (2026-04-10) | PROBABLY LEGACY (Classic-only) | 80% | none | Dies whenever Classic setup page dies. |
| `features/comments` | UI live / backend dead | S1 (3 importers: blocks, assets page, guidelines hub), S7 (localStorage `commentsStore` "v1"; Supabase table unread), S6 (adapter unconsumed) | PROBABLY LEGACY / TRANSITIONAL | 80% | — | See §E live-UI-over-dead-backend. |
| Deck engines older gens: `shared/presentation` v1 + `logo-presentation` + `social-media` builders on `shared/editor` | UI | S2 (all mounted), S1 (load-bearing web of importers), S8 (deck-v2 exists but direction undecided — 03 open-Q1), S10 (April) | PROBABLY LEGACY (frozen, load-bearing) | 70% | undecided | Four deck engines mounted; none deletable until go-forward engine chosen. |

## D. CURRENTLY USED — candidates cleared of suspicion (reconciliations)

| Candidate | Verdict | Evidence |
|---|---|---|
| `src/domains/landing` | **CURRENTLY USED — renders the live `/` page.** | `pages/Index.tsx:5-13` imports 9 section components; `components/layout/{Navbar,Footer}` also import from it. S2: `/` is the eager root route. CLAUDE.md's "dead code kept for reference" is **wrong for v1**; only v2 was dead and it's already deleted. Layer note: internal helpers (`MultiStepEarlyAccess`, `SectionSplit`, `Counter`, `Reveal`) are only reachable through the sections — not separately audited. |
| `features/brand-kit-alt` | **CURRENTLY USED in BOTH UIs — not "Classic bug-fix only".** | Live entry points: `BrandKitPage` (`/a/:slug/brand-kit`), `BrandSettingsPage` (`/a/:slug/settings`), **`BrandSettingsHub` imported by Studio `pages/b/[slug]/settings.tsx:12`**, `LogoVariantCard` imported by domain-layer `brandkit/LogoFilesModule.tsx:38`, `SettingsModule.tsx:13`, `brandGuidePdf.ts` (bulk export via vectorize), `BrandBookSection` (links to blocks). No dead sub-part identified with confidence — the fork's internals are reachable from 4+ live roots. Confirms 03 contradiction #1. |
| `shared/editor` (`EditorWorkspace` + deck engine, `stable/editable-export-v1`) | **CURRENTLY USED — frozen but heavily load-bearing.** | 23 external importer files: social-media, logo-presentation, case-study-deck (3 files), pitch-deck, **`guideline/chronicle/ChronicleGuidelineEditor`** (the CURRENT Chronicle imports it too), guidelines, shared/presentation, all 4 export converters, vectorize, UploadButton, 3 page files. Last touch 2026-08-02 (0c06a23). Emphatically not dead. |
| `shared/services/export/vectorize/*` | **CURRENTLY USED.** | Importers: export converters (svg/pdf), `EditorWorkspace`, `tools/ui-color-system`, `tools/variant-studio`, `brand-kit-alt/brandGuidePdf`. Off-limits tag justified. |
| `features/{analytics,approvals,brand-portal}` | **CURRENTLY USED (frozen) — corrects 03's "route only".** | The **live Studio tools hub** (`pages/b/[slug]/tools.tsx`, a WorkspaceShell nav tab) links to `/p/:slug` (:119), `/b/:slug/analytics` (:160), `/b/:slug/approvals` (:178); plus `searchIndex:69-72` (command palette) and `brand-kit-alt/BrandKitPage:204` (portal). Reachable from primary nav. Backends still dead (§E). |
| `features/{bento,brand-board,mockup-studio,case-study-deck,pitch-deck,tools,tools-page,dev-features,brand-memory,brand-consistency,dam,setup,brand-setup}` | **CURRENTLY USED** | Each has mounted routes and/or multiple live importers (03 verified; spot-rechecked: BRAND_MEMORY 2 consumers, BRAND_CONSISTENCY 1, MOCKUP_TEMPLATES 2). `tools-cosmos` **never existed** (confirmed `ls`). |
| `features/dashboard` — split verdict | `DashboardLayout` + `DashboardNavbar` + `BrandCardMenu` **LIVE** (consumed by `SettingsLayout` [eager, `/settings/*`], `MarketplacePage`, `DashboardShell` → `/dashboard/logo-maker`, `pages/workspace/Home:10`, all G1 pages). `DashboardMain`/`ContinueSurface`/`DashboardSidebar`/**all of `v5/`** DEAD (§A). | Deleting the §A chain must keep the barrel exports of the live components intact. |
| `landingpage/` | **CURRENTLY USED** — separate deployed Vite project; only shared surface is the Supabase project. | 01 §10. |
| Tests | **No test file exclusively tests confirmed-dead code.** Tests inside suspect folders (`shared/editor/historyStore.test.ts`, `brandkit/templateSeeds.test.ts`, `colorEngine.test.ts`, `TemplateGallery.browser.test.tsx`) all cover code with live consumers. 0 test imports of any §A item. | VERIFIED grep across 152 test files. |

## E. Cross-layer mismatches

### Dead/legacy UI sitting on LIVE domain or data
1. **`features/brandkit`** — module-canvas UI is Classic-legacy, but the same folder's `engine/` (colorEngine, brandRules), `templateSeeds`, `TemplateGallery`, `CanvasEditor`, types are imported by **45 external files** including the Studio kit (34 files) and design launchpad. Any deletion pass must split UI from domain first.
2. **`features/guidelines`** — legacy UI generation, but it is the *only* writer/reader of the live `guideline_presentations` Supabase table and feeds the public showcase + exports.
3. **DashboardRoute chain (§A)** — dead pages composed from `features/dashboard`, which also contains live shells (`DashboardLayout` under `/settings/*` of all places). Deleting by folder would break Settings.

### Live UI sitting on DEAD backend (write-only or never-wired persistence)
1. **`shared/utils/localStorage-migration.ts` — live code writing to unread tables.** Called from `useAuth.ts:196,307` on sign-in; INSERTs rows into `comments`, `approvals`, `notifications`, `activity_log` (`:131,167,195,223`). The corresponding Supabase adapters have **zero consumers** — only `activity_log` is ever read back (admin panel via `adminService`). `comments`/`approvals`/`notifications` are **write-only tables**: data goes in at every guest→user migration and nothing in the product reads it.
2. **`features/approvals` + `features/comments` UI** — live routes/panels persisting to localStorage "v1" stores while their Supabase tables (migration 002) and adapters sit idle.
3. **Public bento share** `/brand/:slug/bento/:bentoId` reads in-memory `useBentoStore` (no persist) — live public route over no backend (03 #6, not runtime-tested).
4. **Public design route** `/d/:brandSlug/:designSlug` over localStorage-only DESIGN_STORAGE (03 #7).
5. **Orphaned onboarding-v3 backend** — inverse case: live(?) deployed backend under a UI that no longer exists (all v3 URLs are shims).

## F. Contradictions found by this pass

1. **CLAUDE.md claims `BrandSidebar.tsx` is "dead code kept for reference" — the file was deleted 2026-04-23 (`fa54b0d`)**, together with `OnboardingShell`. Doc stale twice over (it was dead, then removed).
2. **CLAUDE.md claims `src/domains/landing` is dead code — it renders the production `/` page** (confirms 01 §11.8). `src/features/landing-v2` meanwhile no longer exists.
3. **03's onboardingStore importer count includes a dead file**: `useDataSync` has zero callers, so the pre-v4 store has 2 live importers, not 3.
4. **03's "route only" for analytics/approvals/brand-portal understates them** — the Studio tools hub (a primary nav tab) links all three.
5. **03's "no inbound links" for `features/blocks` is wrong** — Classic share page, brand-kit-alt BrandBookSection, and searchIndex all link it.
6. **`pages/dashboard/features/index.tsx` is a self-audit that the docs never mention** — it hand-labels `/dashboard/logo-maker`, `/dashboard/templates`, `/templates`(!), `/marketplace`, `/b/:slug/settings`, logo-presentation, presentations, social-media, approvals, and both old onboarding URLs as `status: 'orphan'`. Its labels partially disagree with reality (e.g. `/templates` and `/b/:slug/settings` are live) — treat as sentiment, not truth.
7. **CLAUDE.md "47 importers" / Step-9 narrative is right that `brandkit/components/editor` and `FabricRenderer` are gone** — verified deleted; the remaining brandkit UI debt is the module canvas + the now-orphaned `BrandKitHub`.

## G. Open questions

1. **Are the 4 orphan edge functions (+ `onboarding-scratch` bucket, `onboarding_rate_limits` table) deployed in prod?** Code-side dead is confirmed; runtime cost/attack surface needs a Supabase dashboard check before dropping migration-007 objects.
2. **Should `comments`/`approvals`/`notifications` writes in `localStorage-migration.ts` be kept** (forward-compat for a Supabase future) or removed as write-only waste? This is the same product decision as 03 open-Q2 (localStorage-first posture).
3. **`/onboarding` link repointing** — 6 live call sites still route through redirect shims; trivial fix, blocks shim deletion.
4. **`pages/dashboard/admin/*`** — delete, or port anything (its analytics page?) into `/admin/*` first? It is URL-reachable without a role gate today.
5. **`features/marketplace` and `features/blocks`** — both frozen since 2026-04-08 with thin-but-real reachability; need a product keep/kill call, not a code call.
6. **`remotion/` and `Brands/`** — archive out of the repo, or git-ignore? Neither is wired to the build; `Brands/` is committing binaries.
7. **Deck-engine consolidation** (blocks §C deletions): until a go-forward engine is chosen, `shared/editor`, `shared/presentation` v1/v2, case-study/pitch-deck all stay frozen-live.
