// Phase A — superseded /onboarding-v3/preview. Canonical lives at /onboard-brand.
import { Navigate } from 'react-router-dom';

export default function OnboardingV3PreviewPage() {
  return <Navigate to="/onboard-brand" replace />;
}
