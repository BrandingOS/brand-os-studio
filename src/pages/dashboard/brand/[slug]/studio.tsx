/**
 * Brand-scoped page wrapper for the AI Consistency Studio.
 * Mounted at `/dashboard/brand/:slug/studio` and `/b/:slug/studio`.
 */
import ConsistencyStudioPage from '@/features/brand-consistency/ui/ConsistencyStudioPage';
export default ConsistencyStudioPage;
