/**
 * Placeholder — Task 8 replaces this wholesale.
 *
 * The shell has no mount point for `renderer.Properties` yet (that is
 * Task 8's job, along with the narrow `EditorSecondaryPanel` guard that
 * lets a properties body render at all). This exists only so the
 * `template-instance` renderer object is complete and the registry test
 * can assert `Properties` is non-null.
 */
import type { DesignPropertiesProps } from '../types';

export function TemplateInstanceProperties(_props: DesignPropertiesProps) {
  return null;
}
