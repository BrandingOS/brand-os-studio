/**
 * Re-exports from the shared editor for backward compatibility.
 * The editor infrastructure now lives in src/shared/editor/.
 * Guidelines-specific content (buildSlides) stays here.
 */
export { EditorWorkspace, type SlideData } from '@/shared/editor';
