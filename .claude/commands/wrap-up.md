Wrap up this coding session. Do ALL of the following in order:

1. **Update `docs/PROGRESS.md`** - Add a new entry at the top with today's date:
   - What was accomplished this session (concrete items, not vague)
   - What's the next logical step to pick up next session
   - Any open questions or blockers
   - Any important decisions made and why

2. **Check for new/changed components** - Scan what we touched this session. If we:
   - Created a new component → add it to `docs/COMPONENTS.md` with its purpose and location
   - Modified an existing component significantly → update its entry
   - Deprecated/deleted a component → remove it from the inventory

3. **Check for new/changed features** - If we:
   - Added a feature → add to `docs/FEATURES.md` with status (active/in-progress/broken)
   - Changed feature status → update it
   - Fixed a broken feature → mark it fixed with date

4. **Check for architectural decisions** - If we made any decision about tech stack, pattern, or approach, add it to `docs/ARCHITECTURE.md` with:
   - The decision
   - The reasoning
   - Alternatives we considered

5. **Update `CLAUDE.md`** - ONLY if we discovered a rule or pattern that should apply to every future session (like "never create X before checking Y"). Don't add session-specific stuff here.

6. **Report back** - After updating, give me:
   - A 3-line summary of what got updated
   - The exact next step for next session
   - Anything I should remember before you clear context

Do NOT ask me questions during this process. Work autonomously. If you're unsure whether something is worth adding, err on the side of adding it briefly.

If any of the docs files don't exist yet, create them with a simple structure.
