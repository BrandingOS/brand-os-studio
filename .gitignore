# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.playwright-mcp/
qa-screenshots/
audit-*.png
test-screenshots/
# Root-level screenshots (drop zone for ad-hoc captures)
/*.png

# Local research / reference assets — not part of the product
new-version/
new-ui/
# Raw brand source files (designer deliverables — fonts, .ai, PDFs).
# Processed assets live in /public/brands/<slug>/.
/Brands/

# Local-only Product OS / internal control center — never commit
/product-os/

# Remotion
remotion/node_modules
remotion/out

.claude/worktrees/

# Vitest browser test failure screenshots
**/__screenshots__/
supabase/.temp/
